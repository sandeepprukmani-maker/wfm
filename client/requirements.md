## Packages
@xyflow/react | Visual workflow graph editor (React Flow successor)
dagre | Graph layout algorithm for auto-layout
js-yaml | YAML parsing and stringifying for workflow definitions
monaco-editor | Code editor for editing YAML definitions
@monaco-editor/react | React wrapper for Monaco editor
framer-motion | Smooth animations for UI elements
date-fns | Date formatting

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
}
